name: CI

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Set rights
      run: chmod +x ./gradlew
    - name: Run check
      run: ./gradlew check
